@using Entity
@model List<GeneralSettings>

@{
    ViewBag.Title = "Index";
}

@section styles{
    <style>
        .create {
            position: absolute;
            right: 10px;
            top: 5px;
        }

        th, td {
            text-align: center;
        }
    </style>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.2/css/all.css" integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr" crossorigin="anonymous">


}
<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12 col-md-12">
                <div class="card">
                    <div class="card-header card-header-warning">
                        <h4 class="card-title">Genel Ayarlar</h4>
                        @if (Model == null)
                        {
                            <a href="~/Panel/GeneralSettings/Create" class="btn btn-warning create">Ayarları Oluştur</a>
                        }
                    </div>
                    <div class="card-body table-responsive">
                        <table class="table table-hover">
                            <thead class="text-warning">
                                <tr>
                                    <th>ID</th>
                                    <th>Başlık</th>
                                    <th>Görüşme Periyodu</th>
                                    <th>Ücret</th>
                                    <th>WhatsApp Danışma</th>
                                    <th>İşlemler</th>
                                </tr>
                            </thead>
                            @*<tbody>
                                    @foreach (DietList item in Model)
                                    {
                                        <tr>
                                            <td>@item.Id</td>
                                            <td>@item.Title</td>
                                            <td>@item.Period</td>
                                            <td>@item.Price.ToString("C")</td>
                                            <td>
                                                @if (item.WhatsAppContanct)
                                                {
                                                    <i class="fas fa-check"></i>
                                                }
                                                else
                                                {
                                                    <i class="fas fa-times"></i>
                                                }
                                            </td>
                                            <td><a href="?del=@item.Id">Sil</a>/<a href="/Panel/DietList/Edit/@item.Id">Düzenle</a></td>
                                        </tr>
                                    }
                                </tbody>*@
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
